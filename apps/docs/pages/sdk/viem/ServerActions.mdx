# Server Actions

Porto provides a set of Server Actions for **Wallet & Account Developers**. These Actions are designed to be used with
a Viem Client that is connected to the [Porto RPC Server](/rpc-server).

```ts twoslash
import { createClient, http } from 'viem' 
import { Chains } from 'porto' 
import { Key, ServerActions } from 'porto/viem'

// Instantiate a Viem Client with Porto-compatible Chain. 
const client = createClient({ 
  chain: Chains.baseSepolia, 
  transport: http(), 
}) 

const account = await ServerActions.createAccount(client, { // [!code focus]
  authorizeKeys: [Key.createSecp256k1()], // [!code focus]
}) // [!code focus]
```

---

| Action | Description |
| ------ | ------ |
| [`createAccount`](/sdk/viem/ServerActions/createAccount) | Create a new Porto Account. |
| [`getCallsStatus`](/sdk/viem/ServerActions/getCallsStatus) | Retrieve the status of a call bundle. |
| [`getCapabilities`](/sdk/viem/ServerActions/getCapabilities) | Retrieve the capabilities of the RPC server. |
| [`getKeys`](/sdk/viem/ServerActions/getKeys) | Retrieve all keys associated with an account. |
| [`health`](/sdk/viem/ServerActions/health) | Retrieve the health of the server. |
| [`prepareCalls`](/sdk/viem/ServerActions/prepareCalls) | Prepare a bundle of calls for execution. |
| [`prepareUpgradeAccount`](/sdk/viem/ServerActions/prepareUpgradeAccount) | Prepare an EOA to be upgraded to a Porto Account. |
| [`sendPreparedCalls`](/sdk/viem/ServerActions/sendPreparedCalls) | Send a prepared and signed call bundle. |
| [`upgradeAccount`](/sdk/viem/ServerActions/upgradeAccount) | Upgrade an EOA to a Porto Account. |
| [`verifySignature`](/sdk/viem/ServerActions/verifySignature) | Verify a signature. |
