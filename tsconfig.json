{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": ["app", "examples", "scripts", "src", "test"],
  "exclude": ["src/_dist"],
  "compilerOptions": {
    "baseUrl": ".",
    "jsx": "react-jsx",
    "paths": {
      "porto": ["src/index.ts"],
      "porto/wagmi": ["src/wagmi/index.ts"]
    }
  }
}
